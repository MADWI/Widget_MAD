<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.mad.widget"
      android:versionCode="1"
      android:versionName="1.0">
      <uses-permission android:name="android.permission.INTERNET" ></uses-permission>
      <uses-permission  android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
      
      <uses-sdk android:minSdkVersion="8" />
      
    <application  android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <!-- Broadcast Receiver that will process AppWidget updates -->
        <receiver android:name=".MadWIWidgetActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
               	<action android:name="com.mad.widget.MadWIWidgetActivity.ACTION_WIDGET_REFRESH"/>
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget" />
        </receiver>
        
        <activity android:name=".PlanChangesActivity">
			<intent-filter>
				<action android:name="com.mad.widget.MadWIWidgetActivity.ACTION_WIDGET_GET_PLAN_CHANGES"/>
			</intent-filter>
		</activity>
		<activity android:name=".MyPreferences">
			<intent-filter>
				<action android:name="com.mad.widget.MadWIWidgetActivity.ACTION_WIDGET_PREFERENCES"/>
			</intent-filter>
		</activity>
    </application>
    
</manifest>